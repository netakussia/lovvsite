# 🚀 Инструкции по развертыванию Love Sites Platform

## Быстрый старт

### 1. Подготовка файлов

```bash
# Скопируйте файлы мультитенантной версии
cp package-multitenant.json package.json
cp server-multitenant.js server.js
cp script-multitenant.js script.js
cp README-multitenant.md README.md
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Миграция данных (если есть существующий сайт)

```bash
# Если у вас уже есть данные в старой версии
node migrate-to-multitenant.js
cp database-multitenant.sqlite database.sqlite
```

### 4. Запуск

```bash
# Режим разработки
npm run dev

# Продакшн режим
npm start
```

## 🌐 Доступные адреса

После запуска сервера:

- **Платформенная админ-панель**: `http://localhost:3000/platform-admin`
- **Админ-панель сайта**: `http://localhost:3000/admin?site=SITE_SLUG`
- **Сайт пользователя**: `http://localhost:3000/site/SITE_SLUG`

## 👤 Первый вход

### Администратор платформы (создается автоматически)
- **Логин**: `admin`
- **Пароль**: `admin123`

### Мигрированный пользователь (если использовали миграцию)
- **Логин**: `migrated_user`
- **Пароль**: `migrated123`
- **URL сайта**: `/site/migrated-love-site`

## 📝 Создание первого сайта

1. Войдите в платформенную админ-панель
2. Зарегистрируйтесь или войдите
3. Перейдите на вкладку "Создать сайт"
4. Заполните форму:
   - **Название сайта**: например, "Мой сайт любви"
   - **Заголовок сайта**: например, "С 4 месяца нас, любимая ❤️"
   - **Подзаголовок**: например, "Подарок только для тебя))"
   - **Шаблон**: выберите "Классический сайт любви"
5. Нажмите "Создать сайт"

## 🎨 Настройка контента сайта

После создания сайта:

1. Откройте админ-панель сайта: `/admin?site=YOUR_SITE_SLUG`
2. Войдите с теми же учетными данными
3. Настройте контент через вкладки:
   - **Секретные посты** - создайте посты с паролем
   - **Таймлайн** - добавьте воспоминания с датами и фото
   - **Галерея** - загрузите фотографии и видео
   - **Временные сообщения** - запланируйте сообщения
   - **Чат** - отредактируйте сообщения чата
   - **Настройки** - измените заголовок и музыку

## 🔧 Переменные окружения

Создайте файл `.env`:

```env
PORT=3000
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
```

## 🐳 Docker развертывание

### Dockerfile

```dockerfile
FROM node:16-alpine

WORKDIR /app

# Копируем package.json и устанавливаем зависимости
COPY package*.json ./
RUN npm install --production

# Копируем исходный код
COPY . .

# Создаем пользователя для безопасности
RUN addgroup -g 1001 -S nodejs
RUN adduser -S nextjs -u 1001
USER nextjs

EXPOSE 3000

CMD ["npm", "start"]
```

### docker-compose.yml

```yaml
version: '3.8'

services:
  love-sites-platform:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - JWT_SECRET=your-super-secret-jwt-key
    volumes:
      - ./database.sqlite:/app/database.sqlite
      - ./images:/app/images
      - ./music:/app/music
    restart: unless-stopped
```

### Запуск с Docker

```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## ☁️ Развертывание на VPS

### 1. Подготовка сервера

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Node.js
curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установка PM2 для управления процессами
sudo npm install -g pm2
```

### 2. Загрузка проекта

```bash
# Клонирование или загрузка файлов
git clone <your-repo> love-sites-platform
cd love-sites-platform

# Установка зависимостей
npm install --production
```

### 3. Настройка PM2

Создайте файл `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [{
    name: 'love-sites-platform',
    script: 'server.js',
    instances: 1,
    exec_mode: 'fork',
    env: {
      NODE_ENV: 'production',
      PORT: 3000,
      JWT_SECRET: 'your-super-secret-jwt-key'
    },
    error_file: './logs/err.log',
    out_file: './logs/out.log',
    log_file: './logs/combined.log',
    time: true
  }]
};
```

### 4. Запуск

```bash
# Создание папки для логов
mkdir logs

# Запуск приложения
pm2 start ecosystem.config.js

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска
pm2 startup
```

### 5. Настройка Nginx (опционально)

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 🔒 Безопасность

### 1. Изменение паролей по умолчанию

```bash
# Войдите в платформенную админ-панель и измените пароль администратора
# Создайте новых пользователей и удалите тестовые аккаунты
```

### 2. Настройка HTTPS

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-nginx

# Получение SSL сертификата
sudo certbot --nginx -d your-domain.com
```

### 3. Настройка файрвола

```bash
# Разрешить только необходимые порты
sudo ufw allow 22    # SSH
sudo ufw allow 80    # HTTP
sudo ufw allow 443   # HTTPS
sudo ufw enable
```

## 📊 Мониторинг

### PM2 мониторинг

```bash
# Статус приложений
pm2 status

# Мониторинг в реальном времени
pm2 monit

# Перезапуск приложения
pm2 restart love-sites-platform

# Просмотр логов
pm2 logs love-sites-platform
```

### Логи приложения

```bash
# Просмотр логов
tail -f logs/combined.log

# Поиск ошибок
grep "ERROR" logs/err.log
```

## 🔄 Обновление

### 1. Создание бэкапа

```bash
# Бэкап базы данных
cp database.sqlite database.sqlite.backup

# Бэкап файлов
tar -czf backup-$(date +%Y%m%d).tar.gz . --exclude=node_modules
```

### 2. Обновление

```bash
# Остановка приложения
pm2 stop love-sites-platform

# Обновление кода
git pull origin main

# Установка новых зависимостей
npm install --production

# Запуск приложения
pm2 start love-sites-platform
```

## 🆘 Устранение неполадок

### Проблемы с базой данных

```bash
# Проверка целостности базы данных
sqlite3 database.sqlite "PRAGMA integrity_check;"

# Восстановление из бэкапа
cp database.sqlite.backup database.sqlite
```

### Проблемы с правами доступа

```bash
# Установка правильных прав
sudo chown -R $USER:$USER /path/to/love-sites-platform
chmod -R 755 /path/to/love-sites-platform
```

### Проблемы с портами

```bash
# Проверка занятых портов
sudo netstat -tulpn | grep :3000

# Завершение процесса на порту
sudo kill -9 $(sudo lsof -t -i:3000)
```

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи приложения
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа к файлам
4. Создайте Issue в репозитории с описанием проблемы

---

**Удачного развертывания! 💕**
