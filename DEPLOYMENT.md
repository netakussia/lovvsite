# 🚀 Инструкции по развертыванию

## Локальный запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Инициализация данных
```bash
node init-data.js
```

### 3. Запуск сервера
```bash
npm start
```

### 4. Доступ к приложению
- **Главная страница**: http://localhost:3000
- **Админ-панель**: http://localhost:3000/admin
- **Логин админа**: admin / admin123

## Развертывание на Heroku

### 1. Подготовка
```bash
# Установите Heroku CLI
# Создайте Procfile
echo "web: node server.js" > Procfile

# Создайте .env файл для production
echo "JWT_SECRET=your-super-secret-jwt-key-here" > .env
```

### 2. Создание приложения
```bash
# Логин в Heroku
heroku login

# Создание приложения
heroku create your-love-site-name

# Установка переменных окружения
heroku config:set JWT_SECRET=your-super-secret-jwt-key-here

# Деплой
git add .
git commit -m "Initial deployment"
git push heroku main
```

### 3. Инициализация базы данных на Heroku
```bash
# После деплоя запустите инициализацию данных
heroku run node init-data.js
```

## Развертывание на Render

### 1. Подготовка репозитория
- Загрузите код в GitHub
- Убедитесь, что есть `package.json` и `Procfile`

### 2. Создание сервиса на Render
1. Зайдите на https://render.com
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command**: `npm install`
   - **Start Command**: `node server.js`
   - **Environment**: Node

### 3. Переменные окружения
Добавьте в настройках сервиса:
- `JWT_SECRET`: ваш секретный ключ для JWT

### 4. Деплой
- Render автоматически задеплоит приложение
- После деплоя запустите инициализацию данных через консоль

## Развертывание на Vercel

### 1. Установка Vercel CLI
```bash
npm i -g vercel
```

### 2. Создание vercel.json
```json
{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ],
  "env": {
    "JWT_SECRET": "your-super-secret-jwt-key-here"
  }
}
```

### 3. Деплой
```bash
vercel --prod
```

## Развертывание на Railway

### 1. Подготовка
```bash
# Установите Railway CLI
npm install -g @railway/cli

# Логин
railway login
```

### 2. Создание проекта
```bash
# Инициализация
railway init

# Установка переменных окружения
railway variables set JWT_SECRET=your-super-secret-jwt-key-here

# Деплой
railway up
```

## Настройка домена

### 1. Покупка домена
- Купите домен на любом регистраторе
- Настройте DNS записи для вашего хостинга

### 2. SSL сертификат
- Большинство платформ предоставляют бесплатный SSL
- Для Heroku: `heroku certs:add`
- Для Render: автоматически
- Для Vercel: автоматически

## Мониторинг и логи

### Heroku
```bash
# Просмотр логов
heroku logs --tail

# Мониторинг
heroku ps
```

### Render
- Логи доступны в панели управления
- Мониторинг в реальном времени

### Vercel
```bash
# Просмотр логов
vercel logs

# Мониторинг
vercel inspect
```

## Резервное копирование

### База данных
```bash
# Создание бэкапа SQLite
cp database.sqlite backup-$(date +%Y%m%d).sqlite

# Восстановление
cp backup-20240101.sqlite database.sqlite
```

### Файлы
- Все изображения должны быть в репозитории
- Регулярно делайте коммиты изменений

## Безопасность

### 1. Смена паролей
```bash
# Создайте новый пароль
node -e "console.log(require('bcryptjs').hashSync('new-password', 10))"

# Обновите в базе данных
sqlite3 database.sqlite "UPDATE admin_users SET password_hash = 'new-hash' WHERE username = 'admin'"
```

### 2. JWT Secret
- Используйте длинный случайный ключ
- Никогда не коммитьте в репозиторий
- Используйте переменные окружения

### 3. Rate Limiting
- Настроено по умолчанию (100 запросов в 15 минут)
- Можно изменить в `server.js`

## Обновление

### 1. Локально
```bash
git pull
npm install
npm start
```

### 2. На хостинге
- Большинство платформ обновляются автоматически при push в репозиторий
- Для ручного обновления используйте соответствующие CLI команды

## Устранение неполадок

### Проблемы с базой данных
```bash
# Пересоздание базы данных
rm database.sqlite
node init-data.js
```

### Проблемы с зависимостями
```bash
# Очистка и переустановка
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с портами
- Убедитесь, что порт 3000 свободен
- Для production используйте переменную окружения `PORT`

## Поддержка

Если возникли проблемы:
1. Проверьте логи сервера
2. Убедитесь, что все зависимости установлены
3. Проверьте переменные окружения
4. Создайте issue в репозитории
