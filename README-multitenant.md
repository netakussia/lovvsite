# 💕 Love Sites Platform - Мультитенантная платформа сайтов любви

Платформа для создания персональных сайтов любви, где каждый пользователь может создать свой собственный сайт с таймлайном, галереей, секретными постами и другими функциями.

## 🚀 Возможности

### Для пользователей:
- **Регистрация и аутентификация** - создание аккаунта на платформе
- **Создание сайтов** - каждый пользователь может создать несколько сайтов
- **Уникальные URL** - каждый сайт получает уникальный адрес вида `/site/username-site-name`
- **Шаблоны сайтов** - выбор из различных шаблонов (классический, упрощенный, для годовщины)
- **Полная настройка** - заголовок, подзаголовок, контент

### Функции каждого сайта:
- **Таймлайн воспоминаний** - хронология важных моментов с фотографиями
- **Галерея** - коллекция фотографий и видео
- **Секретные посты** - контент, защищенный паролем
- **Чат-симуляция** - автоматические сообщения с эффектом печати
- **Временные сообщения** - сообщения, появляющиеся по расписанию
- **Музыкальное сопровождение** - настраиваемая фоновая музыка
- **Темная/светлая тема** - переключение оформления
- **Адаптивный дизайн** - работает на всех устройствах

## 📁 Структура проекта

```
love-sites-platform/
├── server-multitenant.js      # Основной сервер с мультитенантностью
├── database-schema.sql        # Схема базы данных
├── platform-admin.html        # Админ-панель платформы
├── script-multitenant.js      # Фронтенд JavaScript для мультитенантности
├── package-multitenant.json   # Зависимости для мультитенантной версии
├── index.html                 # Шаблон сайта (используется для всех сайтов)
├── gallery.html               # Шаблон галереи
├── admin.html                 # Админ-панель для управления контентом сайта
├── style.css                  # Стили
├── images/                    # Папка с изображениями
├── music/                     # Папка с музыкой
└── README.md                  # Этот файл
```

## 🛠 Установка и запуск

### 1. Установка зависимостей

```bash
# Скопируйте package-multitenant.json в package.json
cp package-multitenant.json package.json

# Установите зависимости
npm install
```

### 2. Инициализация базы данных

База данных будет создана автоматически при первом запуске сервера с использованием схемы из `database-schema.sql`.

### 3. Запуск сервера

```bash
# Режим разработки
npm run dev

# Продакшн режим
npm start
```

Сервер запустится на порту 3000 (или PORT из переменных окружения).

## 🌐 Доступ к платформе

После запуска сервера доступны следующие адреса:

- **Платформенная админ-панель**: `http://localhost:3000/platform-admin`
- **Админ-панель сайта**: `http://localhost:3000/admin?site=SITE_SLUG`
- **Сайт пользователя**: `http://localhost:3000/site/SITE_SLUG`
- **API**: `http://localhost:3000/api/`

## 👤 Управление пользователями

### Создание первого администратора

При первом запуске автоматически создается администратор:
- **Логин**: `admin`
- **Пароль**: `admin123`
- **Email**: `admin@lovesites.com`

### Регистрация новых пользователей

Пользователи могут регистрироваться самостоятельно через платформенную админ-панель или через API:

```bash
curl -X POST http://localhost:3000/api/platform/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "username",
    "email": "user@example.com",
    "password": "password123",
    "fullName": "Имя Пользователя"
  }'
```

## 🏗 Создание сайта

### Через веб-интерфейс

1. Войдите в платформенную админ-панель
2. Перейдите на вкладку "Создать сайт"
3. Заполните форму:
   - Название сайта
   - Заголовок сайта
   - Подзаголовок
   - Шаблон
4. Нажмите "Создать сайт"

### Через API

```bash
curl -X POST http://localhost:3000/api/platform/sites \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "siteName": "Мой сайт любви",
    "siteTitle": "С 4 месяца нас, любимая ❤️",
    "siteSubtitle": "Подарок только для тебя))",
    "templateType": "love_site"
  }'
```

## 📊 Управление контентом сайта

После создания сайта пользователь может управлять его контентом через админ-панель:

1. Откройте админ-панель сайта: `/admin?site=SITE_SLUG`
2. Войдите с учетными данными платформы
3. Управляйте контентом через вкладки:
   - **Секретные посты** - создание постов с паролем
   - **Таймлайн** - добавление воспоминаний с датами
   - **Галерея** - управление фотографиями и видео
   - **Временные сообщения** - планирование сообщений
   - **Чат** - редактирование сообщений чата
   - **Настройки** - изменение заголовка и музыки

## 🔧 API Endpoints

### Платформенные endpoints

- `POST /api/platform/login` - Вход в систему
- `POST /api/platform/register` - Регистрация
- `GET /api/platform/sites` - Список сайтов пользователя
- `POST /api/platform/sites` - Создание нового сайта

### Endpoints сайта (с параметром siteSlug)

- `GET /api/site/:siteSlug/info` - Информация о сайте
- `GET /api/site/:siteSlug/settings` - Настройки сайта
- `GET /api/site/:siteSlug/posts` - Посты таймлайна
- `GET /api/site/:siteSlug/gallery` - Элементы галереи
- `GET /api/site/:siteSlug/chat-messages` - Сообщения чата
- `POST /api/site/:siteSlug/secret-posts/check-password` - Проверка пароля секретного поста
- `POST /api/site/:siteSlug/secret-posts/content` - Получение контента секретного поста
- `GET /api/site/:siteSlug/temporary-messages/status` - Статус временных сообщений

### Админские endpoints сайта

- `PUT /api/site/:siteSlug/admin/settings` - Обновление настроек
- `POST /api/site/:siteSlug/admin/secret-posts` - Создание секретного поста
- `GET /api/site/:siteSlug/admin/secret-posts` - Список секретных постов
- `POST /api/site/:siteSlug/admin/posts` - Создание поста таймлайна
- `PUT /api/site/:siteSlug/admin/chat-messages` - Обновление сообщений чата

## 🎨 Шаблоны сайтов

### love_site (Классический)
Полный набор функций: таймлайн, галерея, секретные посты, чат, музыка, временные сообщения.

### simple_love (Упрощенный)
Основные функции: таймлайн, галерея, чат.

### anniversary (Годовщина)
Акцент на таймлайне: таймлайн, галерея, музыка.

## 🔒 Безопасность

- **JWT токены** для аутентификации
- **Хеширование паролей** с bcrypt
- **Rate limiting** для защиты от злоупотреблений
- **Валидация данных** на сервере
- **Проверка прав доступа** к сайтам

## 🚀 Развертывание

### Переменные окружения

```bash
PORT=3000                    # Порт сервера
JWT_SECRET=your-secret-key   # Секретный ключ для JWT
```

### Docker (опционально)

```dockerfile
FROM node:16-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
```

## 📝 Миграция с одиночного сайта

Если у вас уже есть одиночный сайт любви, вы можете:

1. Сохранить старую версию в папке `legacy/`
2. Запустить мультитенантную версию параллельно
3. Использовать команды `npm run legacy` и `npm run legacy-dev` для старой версии

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.

## 💝 Поддержка

Если у вас есть вопросы или предложения, создайте Issue в репозитории или свяжитесь с разработчиками.

---

**Создано с любовью для всех влюбленных пар 💕**
