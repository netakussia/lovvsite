# Love Site - Сайт любви с админ-панелью

Красивый сайт-подарок с возможностью управления контентом через админ-панель.

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск сервера
```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

### 3. Открыть сайт
- Главная страница: http://localhost:3000
- Админ-панель: http://localhost:3000/admin

## 🔐 Доступ к админ-панели

**По умолчанию:**
- Логин: `admin`
- Пароль: `admin123`

⚠️ **ВАЖНО:** Смените пароль в production!

## 📁 Структура проекта

```
├── server.js              # Основной сервер Node.js
├── admin.html             # Админ-панель
├── index.html             # Главная страница
├── script.js              # Фронтенд JavaScript
├── api-client.js          # API клиент
├── style.css              # Стили
├── package.json           # Зависимости
├── database.sqlite        # База данных (создается автоматически)
└── images/                # Изображения
```

## 🛠️ Функциональность

### Публичная часть
- Красивая анимация сердечек
- Счетчик дней отношений
- Симуляция чата с сообщениями
- Таймлайн воспоминаний с фотографиями
- Секретные сообщения с паролем
- Музыкальный плеер
- Переключение темы (светлая/темная)

### Админ-панель
- **Поздравления**: создание, редактирование, удаление
- **Посты**: управление таймлайном воспоминаний
- **Чат**: редактирование сообщений чата
- **Приватный контент**: возможность скрыть контент от неавторизованных пользователей

## 🔒 Безопасность

- JWT токены для авторизации
- Хеширование паролей с bcrypt
- Rate limiting для API
- Приватный контент не отображается без авторизации

## 🌐 Развертывание

### Heroku
1. Создайте приложение на Heroku
2. Подключите GitHub репозиторий
3. Установите переменную окружения `JWT_SECRET`
4. Деплой

### Render
1. Подключите GitHub репозиторий
2. Выберите Node.js
3. Установите переменную окружения `JWT_SECRET`
4. Деплой

### Локально
```bash
git clone <your-repo>
cd MyLoveSite
npm install
npm start
```

## 📝 API Endpoints

### Публичные
- `GET /api/greetings` - Получить поздравления
- `GET /api/posts` - Получить посты
- `GET /api/chat-messages` - Получить сообщения чата

### Админ (требуют авторизации)
- `POST /api/admin/login` - Вход
- `POST /api/admin/greetings` - Создать поздравление
- `PUT /api/admin/greetings/:id` - Обновить поздравление
- `DELETE /api/admin/greetings/:id` - Удалить поздравление
- `POST /api/admin/posts` - Создать пост
- `PUT /api/admin/posts/:id` - Обновить пост
- `DELETE /api/admin/posts/:id` - Удалить пост
- `PUT /api/admin/chat-messages` - Обновить сообщения чата

## 🎨 Кастомизация

### Изменение дизайна
Отредактируйте `style.css` для изменения внешнего вида.

### Добавление новых функций
1. Добавьте новые эндпоинты в `server.js`
2. Обновите админ-панель в `admin.html`
3. Интегрируйте с фронтендом через `api-client.js`

### Изменение базы данных
Схема базы данных находится в `server.js` в функции `db.serialize()`.

## 🐛 Устранение неполадок

### Проблемы с базой данных
- Удалите `database.sqlite` и перезапустите сервер
- Проверьте права доступа к файлу

### Проблемы с авторизацией
- Очистите localStorage в браузере
- Проверьте JWT_SECRET в переменных окружения

### Проблемы с загрузкой изображений
- Убедитесь, что изображения находятся в папке `images/`
- Проверьте правильность путей в админ-панели

## 📄 Лицензия

MIT License - используйте свободно для личных проектов.

## 💝 Создано с любовью

Этот проект создан как подарок для любимого человека. Надеюсь, он принесет радость и поможет сохранить прекрасные воспоминания!
